liste = list(filter(lambda x: x > 10, [42, 3, 4, 7]))

# Construit une nouvelle liste contenant les éléments de la liste en entrée supérieurs à 10
# Ici on aura une liste avec un seul élément [42]

print(liste)
